//mapping.cpp
//
//

#include <fstream>
#include "mapping.h"

/*midimap parse_mapping()
{
	midimap mapping;
	//open file "config", and read number of pages
	for (int i=0; i!=3number of pages; ++i)
		mapping.push_back(map<int,map<int,Operation>> {});
	//open file "page1", and read line-by-line, populating mapping vector
	//set target (model, controller)
	mapping[0][2][3].set_target(0);
	return mapping;
}*/
